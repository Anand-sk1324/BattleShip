const player = require('./player');

function gameManager(){
    const p1 = player();
    const cpu = player();

    let gameBoard = false;

    function gameLoop(){
        while(!gameOver){
            
        }
    }
} 
module.exports = gameManager;